[features]
hide = [ "portaudio_backend", "rodio_backend", "rodiojack_backend" ]

[[package.extra-patches]]
number = 10
file = "0001-relax-dependencies-on-directories-and-gethostname.patch"
comments = [ "Relax version requirements for dependencies shipped in Fedora" ]

[[package.extra-patches]]
number = 11
file = "0002-switch-to-syslog-6.patch"
comments = [ "Switch dep to syslog 6 because syslog 7 is not yet in Fedora " ]
